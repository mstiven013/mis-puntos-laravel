jQuery(function($) {
	$(document).ready(function(){

		$('.modal').modal(); // Initialize all materialize modals
		
    	$('select').formSelect(); // Initialize all materialize select lists
    	
	});
});